[project]
name = "attack-surface-management"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "bbot",
    "neo4j>=5.28.1",
    "aiodocker>=0.24.0",
    "pillow>=11.3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "mypy>=1.17.0",
    "ruff>=0.12.5",
]
