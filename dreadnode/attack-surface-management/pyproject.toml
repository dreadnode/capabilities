[project]
name = "attack-surface-management"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "bbot>=2.0",
    "neo4j>=5.28.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "mypy>=1.17.0",
    "ruff>=0.12.5",
]
